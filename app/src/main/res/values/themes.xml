<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.Trainex" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="background">@color/background</item>
        <item name="backgroundHeader">@color/backgroundHeader</item>
        <item name="selection">@color/selection</item>
        <item name="link">@color/link</item>
        <item name="android:textColor">@color/text</item>
        <item name="backgroundPrimaryButton">@color/backgroundPrimaryButton</item>
        <item name="backgroundSecondaryButton">@color/backgroundSecondaryButton</item>
        <item name="textSecundaryButton">@color/text</item>
        <item name="textPrimaryButton">@color/textPrimaryButton</item>
        <item name="separator">@color/separator</item>
        <item name="backgroundSearch">@color/backgroundSearch</item>
        <item name="backgroundCardView">@color/backgroundCardView</item>
        <item name="primary">@color/primary</item>
        <item name="secondary">@color/secondary</item>

    </style>

    <style name="ShapeableImageView_Radius">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">40dp</item>
    </style>

    <style name="Theme.Trainex" parent="Base.Theme.Trainex" />
</resources>
